#include "main.h"
/**
* print_last_digit - return last digit
* @n : number to check
* Return:0 or 1
*/
int print_last_digit(int n)
{
int nb;
if (b < 0)
{
nb = -1 * (n % 10);
}
else{
nb = n % 10;
}
_putchar(nb + '0');
return (nb);
}
